query RecipeAndIngredients($recipeId: String!) {
	recipe(id: $recipeId) {
		id
		title
		tutorialVideo
		difficulty
		portion
		steps
		cookingTime
		creator {
			id
      		email
		}
		ingredients {
			name
			quantity
			mesureUnit
			previewPicture
		}
		tags {
			name
		}
	}
}
